# Example Docker environment file
# Copy this to .env and fill in your actual values

# Required: Get your bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Required: Your Telegram user ID (get from @userinfobot)
ADMIN_IDS=123456789

# Database - SQLite for production (default)
DATABASE_URL=sqlite:///data/database.db

# Database - PostgreSQL for development (uncomment if using docker-compose.dev.yml)  
# DATABASE_URL=postgresql://newsbot:newsbot_dev@postgres:5432/newsbot_dev

# Redis for FSM storage (handled by docker-compose)
REDIS_URL=redis://redis:6379/0

# Optional: Logging level
LOG_LEVEL=INFO

# Optional: Update frequency (seconds)
SCHEDULER_INTERVAL=300

# Optional: Items per update
MAX_ITEMS_PER_UPDATE=5

# Optional: HTTP settings
REQUEST_TIMEOUT=30
MAX_RETRIES=3
RETRY_DELAY=5
USER_AGENT=Mozilla/5.0 FeedAggregatorBot/1.0